// Intentionally empty. The desktop shell is a thin wrapper around the live
// site; nothing exposes Node APIs to the renderer. Kept as a file so the
// BrowserWindow preload path resolves and contextIsolation stays enforced.
